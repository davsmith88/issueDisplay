
	<% if @solution.errors.any? %>
		<div id='error_explanation'>
			<h2>
				<%= pluralize(@solution.errors.count, "error") %> prohibited this solution from being saved
			</h2>
			<ul>
				<% @solution.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class='form-group'>
		<%= form.label :name, class: "col-sm-4 control-label" %>
		<div class='col-sm-8'>
			<%= form.text_field :name, class: "form-control" %>
		</div>
	</div>

	<div class='form-group'>
		<%= form.label :content, class: "col-sm-4 control-label" %>
		<div class='col-sm-8'>
			<%= form.text_field :content, class: "form-control" %>
		</div>
	</div>

	<div class='form-group'>
		<div class='col-sm-offset-4 col-sm-8'>
			<%= form.submit "Create Solution", class: "btn btn-default" %>
		</div>
	</div>

	


