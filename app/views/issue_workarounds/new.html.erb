<h3>Add Workaround to <%= @issue_workaround.issue.name %></h3>
<br>
<%= form_for [@issue, @issue_workaround], builder: MyFormBuilder, html: {class: 'form-horizontal'} do |form| %>
	<%= render partial: 'form', locals: {form: form} %>
<% end %>