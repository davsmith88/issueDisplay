console.log('ajax reviews#new')

var modal = $('#quickModal');

modal.find('#myModalLabel').text('Add a new <%= @label %>');
modal.find('#modal_body').html("<%= j (render partial: 'reviews/form') %>");
modal.modal('show');