
<%= form_for [@issue, @review], remote: true, builder: MyFormBuilder, html: {class: "form-horizontal"} do |form|  %>
	<%= form.text_area :description %>
	<%= form.submit "Save #{@class}", label: "Action", class: "btn btn-default" %>
<% end %>
