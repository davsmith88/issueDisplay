<p>
	<% if activity.trackable %>
	  	<% if activity.owner_id == current_user.id %>
			You 
		<% else %>
			<%= activity.owner.name %> 
		<% end %>
		created an issue  :
	  <%= link_to activity.trackable.name, activity.trackable %>
	<% else %>
	  Created an issue, which has since been removed
	<% end %>
</p>