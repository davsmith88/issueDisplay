var modal = $('#quickModal');

modal.find('.modal-title').text("Create a new Step")
modal.find('#modal_body').html('<%= j (render partial: "quick_add",locals: {place: "new"}) %> ');
modal.modal('show');
